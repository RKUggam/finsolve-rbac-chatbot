# Optional NER-based PII detection (Microsoft Presidio + spaCy model).
# The app works without this — app/guardrails/pii.py falls back to regex — but
# installing it adds detection of names, locations, and other entities.
#
#   pip install -r requirements-optional.txt
#   python -m spacy download en_core_web_lg
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.8.3
